import SwiftUI

enum CashTheme {
    static let green = Color(red: 0 / 255, green: 199 / 255, blue: 47 / 255)
    static let darkBg = Color(red: 18 / 255, green: 18 / 255, blue: 18 / 255)
    static let cardBg = Color(red: 28 / 255, green: 28 / 255, blue: 30 / 255)
    static let subtleText = Color.white.opacity(0.55)
}
